---
import Section from "../Section.astro";
import { projects, projectsB } from "@landing";

import {
  Rss,
  Github,
  Html5,
  Css3,
  React,
  Javascript,
  Python,
} from "simple-icons-astro";
import Circle from "@/icons/circle.astro";
import AstroIcon from "@/icons/AstroIcon.astro";

const SKILL_ICONS: Record<string, any> = {
  Html5,
  Css3,
  React,
  Javascript,
  AstroIcon,
};
---

<Section>
  <h2>Proyectos</h2>
  <article class="text-description">
    <p>
      Los pequeños detalles marcan una gran diferencia. Con los años he
      aprendido a fijarme en esos detalles. Nunca dejo de aprender. He aquí
      varias recopilaciones a lo largo de este camino.
    </p>
    <div class="box-left">
      <a href="#" class="rss-btn">
        <Rss class="w-3 h-3" />
        Suscríbete
      </a>
    </div>
  </article>

  <article class="projects-item">
    <header>
      <h3>
        Con HTML, CSS y JS
        <span class="text-red-400"><Circle /></span>
        <span class="text-blue-400"><Circle /></span>
        <span class="text-yellow-500"><Circle /></span>
      </h3>
      <p>
        Presento alguno de mis proyectos destacados y el enfoque que tomé en
        cada uno de ellos.
      </p>
    </header>

    <ul class="project-container">
      {
        projects.map(
          ({ url, description, highlights, name, isActive, github, skill }) => {
            const Icon = SKILL_ICONS[skill];
            return (
              <li>
                <article class="item-project">
                  {Icon && <Icon class="w-4 h-4" />}
                  <header>
                    <a href={url} target="_blank">
                      {name}
                    </a>
                    {isActive && <span>•</span>}
                    {github && (
                      <a href="">
                        <Github class="w-4 h-4" />
                      </a>
                    )}
                  </header>
                </article>
              </li>
            );
          }
        )
      }
    </ul>
  </article>

  <article class="projects-item">
    <header>
      <h3>
        Con Frameworks
        <span class="text-green-400"><Circle /></span>
        <span class="text-red-600"><Circle /></span>
        <span class="text-purple-500"><Circle /></span>
      </h3>
      <p>
        Algunos de mis proyectos, prácticas que brindan una idea de mi
        experiencia y habilidades, asi como de la variedad de soluciones que
        puedo ofrecer.
      </p>
    </header>
    <ul class="project-container">
      {
        projectsB.map(
          ({ url, description, highlights, name, isActive, github, skill }) => {
            const Icon = SKILL_ICONS[skill];
            return (
              <li>
                <article class="item-project">
                  {Icon && <Icon class="w-4 h-4" />}
                  <header>
                    <a href={url} target="_blank">
                      {name}
                    </a>
                    {isActive && <span>•</span>}
                    {github && (
                      <a href={github} target="_blank">
                        <Github class="w-4 h-4" />
                      </a>
                    )}
                  </header>
                </article>
              </li>
            );
          }
        )
      }
    </ul>
  </article>
</Section>

<style>
  h2 {
    margin-bottom: 1.5rem;
    font-weight: 800;
    line-height: 1.5;
    font-size: 20px;
    text-transform: uppercase;
    background-color: #1e252c;
    color: #f8fafb;
    width: fit-content;
    padding: 1px 32px 1px 16px;
  }

  .text-description {
    border: 1px solid rgba(30, 37, 44, 10%);
    padding: 1rem;
    margin-bottom: 24px;
  }

  .text-description p {
    font-size: 0.75rem;
    text-wrap: pretty;
    font-weight: 300;
    color: rgba(30, 37, 44, 80%);
  }

  .box-left {
    display: flex;
    gap: 0.25rem;
    justify-content: end;
    font-size: 0.625rem;
  }

  .rss-btn {
    display: flex;
    align-items: center;
    gap: 0.25rem;
    padding: 4px;
    background: rgba(30, 37, 44, 8%);
    color: rgba(30, 37, 44, 60%);
  }

  .projects-item {
    border: 1px solid rgba(30, 37, 44, 10%);
    border-radius: 0.5rem;
    padding: 1rem;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    margin-bottom: 24px;
  }

  .projects-item header h3 {
    display: flex;
    align-items: center;
    gap: 4px;
    font-size: 14px;
    font-weight: 600;
    text-wrap: pretty;
  }

  .projects-item header p {
    font-size: 0.75rem;
    font-weight: 400;
    color: rgba(30, 37, 44, 80%);
  }

  .project-container {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-inline: -8px;
  }

  .item-project {
    border: 1px solid rgba(30, 37, 44, 10%);
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.125rem 0.25rem;
    width: fit-content;
    height: 100%;

    transition: all 0.3s ease;
  }

  .item-project:hover {
    background-color: rgba(30, 37, 44, 8%);
  }

  .item-project header {
    display: flex;
    align-items: center;
    gap: 0.25rem;
  }

  .item-project header a {
    font-size: 0.75rem;
    color: rgba(30, 37, 44, 65%);
  }

  .item-project header a:hover {
    text-decoration: underline;
  }

  .item-project header span {
    color: rgb(0, 188, 47);
    font-size: 2rem;
    line-height: 0.85;
  }

  @media (width <= 700px) {
    .project-container {
      margin-inline: 0px;
      margin-top: 2rem;
    }
  }
</style>
